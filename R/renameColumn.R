# renameColumn
#
#


renameColumn <- function(df,old,new) {
  print(old)
  print(new)
  print(df)
 # if (is.numeric(old))
 #   names(df)[old]<-new
 # else
 #   colnames(df)[colnames(df)==old] <- new
}
